const PREFIX = '__WAY_UI_SESSION__';

export const SESSION_KEYS = {
  // 路由配置
  ROUTE_CONFIGS: `${PREFIX}route_configs`,
  // 子系统名称，例如：unifiedauth
  APP_NAME: `${PREFIX}app_name`,
  // 子系统标题，例如：统一认证
  APP_TITLE: `${PREFIX}app_title`,
  DEPT_ID: `${PREFIX}dept_id`,
  UNIT_CODE: `${PREFIX}unit_code`,
};
