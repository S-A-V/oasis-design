import { withInstall } from '@way-ui/internal-utils';
import Layout from './src/index.vue';
import InnerLink from './src/components/InnerLink/index.vue';

export const WLayout = withInstall(Layout);

export const WInnerLink = withInstall(InnerLink);
